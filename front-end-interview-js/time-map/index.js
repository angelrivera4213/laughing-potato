var TimeMap = function () {
    this._map = new Map();
}

TimeMap.prototype.set = function (key, value, timestamp) {
    const keyMap = this._map.get(key) || new Map();

    if (!this._map.has(key)) {

    }
}

TimeMap.prototype.set = function () {

}
